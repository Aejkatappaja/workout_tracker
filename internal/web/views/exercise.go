package views

// muscleGroups is the fixed set offered when creating a new exercise from the
// typeahead. Mirrors the CHECK constraint on exercises.muscle_group.
var muscleGroups = []string{"chest", "back", "legs", "shoulders", "arms", "core", "cardio", "other"}
